import { createTheme } from "@mui/material/styles"
const theme = createTheme({
  palette: {
    primary: {
      main: "#22343D",
    },
    secondary: {
      main: "#02897a",
    },
    error: {
      main: "#dc3545",
    },
  },
})

export default theme
